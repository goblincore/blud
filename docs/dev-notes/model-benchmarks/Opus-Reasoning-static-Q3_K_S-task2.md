<|think|>
Let me work through this systematically to generate the solution.

I need to create a TypeScript Vec2 class with:
1. Constructor with x,y
2. Immutable operations that return new Vec2 instances
3. Static constants
4. Comprehensive vitest tests

The class needs methods like add, sub, mul, len, lenSq, normalize, dot, lerp, dist, angle, rotate, equals, clone, and static constants.

I'll implement the Vec2 class first, then write thorough tests covering all edge cases.
<|/think|>
