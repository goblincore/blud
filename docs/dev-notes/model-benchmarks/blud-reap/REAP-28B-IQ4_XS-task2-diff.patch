diff --git a/src/engine/rng.test.ts b/src/engine/rng.test.ts
index 616178b..08023a9 100644
--- a/src/engine/rng.test.ts
+++ b/src/engine/rng.test.ts
@@ -83,6 +83,27 @@ describe('SeededRng', () => {
     expect(original).toEqual([1, 2, 3, 4, 5, 6, 7, 8]); // input not mutated
   });
 
+  it('nextGaussian returns approximately standard normal over many samples', () => {
+    const rng = new SeededRng(42);
+    const n = 10000;
+    const samples: number[] = [];
+
+    for (let i = 0; i < n; i++) {
+      samples.push(rng.nextGaussian());
+    }
+
+    // Sample mean
+    const sum = samples.reduce((a, b) => a + b, 0);
+    const mean = sum / n;
+
+    // Sample stddev
+    const variance = samples.reduce((a, b) => a + (b - mean) ** 2, 0) / n;
+    const stddev = Math.sqrt(variance);
+
+    expect(Math.abs(mean)).toBeLessThan(0.1);
+    expect(Math.abs(stddev - 1)).toBeLessThan(0.1);
+  });
+
   it('fork produces independent instances from same state', () => {
     const rng1 = new SeededRng(12345);
     const rng2 = rng1.fork();
diff --git a/src/engine/rng.ts b/src/engine/rng.ts
index 0c02fea..5e66bca 100644
--- a/src/engine/rng.ts
+++ b/src/engine/rng.ts
@@ -1,5 +1,7 @@
 export class SeededRng {
   private state: number;
+  private _gaussianSpare: number = 0;
+  private _hasGaussianSpare: boolean = false;
 
   constructor(seed: number) {
     // Ensure seed is treated as unsigned 32-bit integer
@@ -60,6 +62,22 @@ export class SeededRng {
     return arrCopy;
   }
 
+  nextGaussian(): number {
+    let u1, u2, s;
+    do {
+      u1 = 2 * this.next() - 1;
+      u2 = 2 * this.next() - 1;
+      s = u1 * u1 + u2 * u2;
+    } while (s >= 1 || s === 0);
+
+    const factor = Math.sqrt(-2 * Math.log(s) / s);
+
+    // Cache the second value for next call
+    this._gaussianSpare = u2 * factor;
+    this._hasGaussianSpare = true;
+    return u1 * factor;
+  }
+
   fork(): SeededRng {
     // Create a new instance with the current state as seed
     // This ensures the new RNG is independent but derived from current state
