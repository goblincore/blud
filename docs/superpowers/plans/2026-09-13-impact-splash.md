# Reference-directed slug impact splash

User likes Smooth reconstruction but rejects slug burst as a blob that drops. Reference screenshot shows explosive outward/upward crown, torn sheets, long tapering fingers and detached droplets. Reference https://cghow.com/blood-in-ue-5-1-niagara-tutorial/ text describes texture masks, panning/dissolve, normal detail, randomized velocity/size and layers. Do NOT claim it requires baked FLIP meshes. Implement an original procedural version, no copying paid assets.

Continue prior blood comparison in ISOLATED dispatcher worktree. Baseline branch contains reviewer corrections: comparison starts mid-jet, collapsible UI, surface anchors, burst now slug impact at [0,1.35,.55], smooth renderer accepted as better by user. Primary checkout defaults and existing preview service 5413 must not be touched. Do not touch .lab-tmp, training/models/private QA, node_modules or unrelated services. No merge/push. Commit scoped files. Use CPU tests/typecheck; do NOT run GPU/browser/benchmark concurrently (parent coordinates visual QA). No subagents.

Root cause: IMPACT_GOUT.slug uses speed .2-.5 m/s, 85 droplets, intentionally tuned to a near stationary mass. Earlier 8 m/s case was rejected as needles. Preserve that existing scenario/settings as Current. Do not globally increase all blood speeds or mutate IMPACT_GOUT from comparison (shared global). New effect should be supplementary and independently selectable.

Implement:
1. A shared production-capable impact-splash module with explicit event origin, direction, deterministic seed, elapsed time and bounded lifetime. Create genuine thin curved fan/crown surface patches/mesh strips oriented about impact axis, expanding rapidly at first then slowing/curling and breaking apart. Several unequal lobes and tapering fingers, sparse detached droplets. Scale should read as a violent wound burst, not a room-filling explosion. Gravity takes over after expansion rather than immediate clump fall. Use a coherent directional envelope (initial .1-.25s expansion, .2-.6s tear/drop as starting artistic timings).
2. Sheet silhouettes must show connected broad areas with ragged edges/holes. Procedural material-space masks/dissolve and smooth normals/wet highlights. No world-space swimming, flat square cards, unlit triangle sails, mist obscuring the whole effect or solely more density blobs. True shading/depth respect. Prefer bounded procedural meshes + seeded material mask over a new fluid sim. Current goo droplets/pools remain independent. Normalize effect direction so wall/body impacts orient outwards; floor example is not always world-up.
3. Comparison page add clearly named Current slug / Impact splash choice that preserves identical camera/seed/timing. Keep Original/Smooth reconstruction independent; don't confuse shape variant with filtering. New effect preview freezes at a representative crown moment and loops, has reset; use visible front-of-proxy origin and close framing. Leave a concise status legend/time indicator. Add production-facing event API and optional game integration only behind new explicit flag, with no unrequested default promotion. If game integration would require broad changes document exact wiring needed rather than fake a successful game test.
4. Meaningful CPU tests for stable seeds, direction transform, finite normals/positions, lifetime cleanup/budget, and independent baseline (no global tuning mutation). Run npx tsc --noEmit and focused vitest single worker. Do not replace validation with source-text assertions.
5. Report exact changes/controls and visual QA pending. Commit notes at docs/dev-notes/2026-09-13-impact-splash.md. It is NOT accepted until actual rendered crown is visible and parent/user compare in lab, then game. Don't describe the old blob as a successful crown merely because tests pass.

Allowed scope: new webgpu/impact-splash*.ts, blood-compare-main.ts/html and related focused tests, minimal opt-in game-main wiring if safe. Keep original renderer, original spawnImpactGout profiles and user-liked smooth path intact. Run no servers. Parent will review branch, integrate into isolated preview then run WebGPU checks.

## Additional user animation reference

User supplied /Users/donny/Pictures/bloodreerence/blood_Fx_ue5_niagara (1).webp. Animated WebP: 120 frames, 3821 ms; roughly three repeated bursts. Reviewer contact sheet /tmp/blood-reference-contact.png. Visual envelope: rapid compact crown emergence, broad irregular sheet fingers by roughly 0.2-0.4 seconds into each cycle, tearing/dissolving into fragments and red fine spray by ~0.5-0.8s, mostly detached droplets by ~1s. These are approximate image observations, not physical simulation measurements. Crucial: mass loses continuity before merely falling as a blob. Crown expansion and sheet breakup matter more than increasing bead count. Darker solid sheets + brighter translucent fine spray; avoid uniformly opaque red lump. Adapt orientation to outward wound direction, not always ground-up.
